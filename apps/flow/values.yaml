image:
  registry: zot.zot.svc.cluster.local:5000
  tag: dev

hostname: flow-dev.turdblossom.dev

existingSecret: flow-dev-secrets

# louise has had intermittent network/reboot issues; tina has been stable.
nodeSelector:
  kubernetes.io/hostname: tina
