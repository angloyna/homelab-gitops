apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-realtime
  labels:
    {{- include "flow.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Release.Name }}-realtime
  ports:
    - port: 4000
      targetPort: 4000
