apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api-public
  labels:
    {{- include "flow.labels" . | nindent 4 }}
spec:
  selector:
    app: {{ .Release.Name }}-api-public
  ports:
    - port: 8000
      targetPort: 8000
